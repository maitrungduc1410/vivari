[package]
name = "open-webcontainer-codec"
version = "0.1.0"
edition = "2021"
description = "OpenContainer compression codec (zlib/deflate/gzip + brotli) compiled to WebAssembly, running in-process beneath Node's real lib/zlib.js"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
# miniz_oxide backend = pure Rust, compiles cleanly to wasm32-unknown-unknown.
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }

[profile.release]
opt-level = "s"
lto = true
